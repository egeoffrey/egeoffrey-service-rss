package: myhouse-service-rss
revision: 2
version: development
git: myhouse-project/myhouse-service-rss
modules:
  - service/rss
default_config:
  - service/gpio_raspi:
      mode: bcm
  - sensors/templates/rss:
      disabled: true
      description: rss
      format: string
      retain: single_value
      service:
        configuration:
          url: http://rss.cnn.com/rss/cnn_latest.rss
        mode: active
        name: rss
        schedule:
          minutes: 60
          trigger: interval